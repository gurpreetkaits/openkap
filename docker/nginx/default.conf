server {
    listen 80;
    server_name localhost;
    root /var/www/html/public;
    index index.php index.html;

    # Max upload size (for video uploads)
    client_max_body_size 10G;

    # Timeouts for large video uploads
    client_body_timeout 1800s;
    send_timeout 1800s;
    proxy_read_timeout 1800s;
    proxy_connect_timeout 1800s;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;

        # Timeouts for PHP processing
        fastcgi_read_timeout 1800s;
        fastcgi_send_timeout 1800s;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2|ttf|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }

    # HLS Playlists (CORS enabled) - try static file first, then fallback to PHP
    location ~* \.m3u8$ {
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        add_header Cache-Control "no-cache";
        try_files $uri /index.php?$query_string;
    }

    # HLS Segments (CORS enabled) - try static file first, then fallback to PHP
    location ~* \.ts$ {
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Methods "GET, OPTIONS";
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri /index.php?$query_string;
    }

    # Video streaming optimization
    location ~* \.(mp4|webm|mov)$ {
        expires 1y;
        add_header Cache-Control "public";
        add_header Accept-Ranges bytes;
    }
}
