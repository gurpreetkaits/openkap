{
  "name": "openkap-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for OpenKap - access video transcriptions from Claude and other AI tools",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "npx tsx server.ts",
    "build": "npx tsc && node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.4"
  }
}
